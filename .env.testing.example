# PostgreSQL for integration tests ONLY — must be a separate database from dev.
# Tests drop and recreate the crawled_pages table on every run.
# NEVER point this at a development or production database.
# Copy to .env.testing and fill in your values.
CRAWLER_TEST_POSTGRES_DSN=postgresql://localhost/crawler_test
